README is not complete (have to add gdrive link to apps contents)

1. Preprocess the file using `./process.sh`
2. Make sure to give +x permission to `run.sh`, `eval.sh` and `apps/multi-bleu.perl`
3. Run `eval.sh`